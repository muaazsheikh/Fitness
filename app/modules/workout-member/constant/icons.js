let baseIconUrl = "../../../assets/icons/";

const notes_edit = require(baseIconUrl + `notes_edit.png`);
const star_icon = require(baseIconUrl + `star_icon.png`);
const star_gray = require(baseIconUrl + `star_gray.png`);
const up_icon = require(baseIconUrl + `up_icon.png`);
const down_icon = require(baseIconUrl + `down_icon.png`);
const right_arrow = require(baseIconUrl + `right_arrow.png`);
const down_white_icon = require(baseIconUrl + `down_white_icon.png`);
const review_place = require(baseIconUrl + `review_place.png`);
const review = require(baseIconUrl + `review.png`);
const completed_icon = require(baseIconUrl + `completed_icon.png`);
const uncompleted_icon = require(baseIconUrl + `uncompleted_icon.png`);
const Session_icon = require(baseIconUrl + `Session_icon.png`);
const noteIcon = require(baseIconUrl + `noteIcon.png`);
const protien_icon = require(baseIconUrl + `protien_icon.png`);
const left_arrow = require(baseIconUrl + `left_arrow.png`);
const carb_icon = require(baseIconUrl + `carb_icon.png`);
const fat_icon = require(baseIconUrl + `fat_icon.png`);
const fiber_icon = require(baseIconUrl + `fiber_icon.png`);
const white_star = require(baseIconUrl + `white_star.png`);
const edit_white = require(baseIconUrl + `edit_white.png`);
const plus_icon = require(baseIconUrl + `plus_icon.png`);
const file = require(baseIconUrl + `file.png`);
const shoulder = require(baseIconUrl + `shoulder.png`);
const leg = require(baseIconUrl + `leg.png`);
const tab_workout = require(baseIconUrl + `tab_workout.png`);
const chat_icon = require(baseIconUrl + `chat_icon.png`);
const tab_member = require(baseIconUrl + `tab_member.png`);
const trainer_comment = require(baseIconUrl + `trainer_comment.png`);
const member_comment = require(baseIconUrl + `member_comment.png`);
const write_icon = require(baseIconUrl + `member_comment.png`);
const req_write = require(baseIconUrl + `req_write.png`);
const req_confirm = require(baseIconUrl + `req_confirm.png`);
const CrossButton = require("../../../assets/icons/CrossButton.png");

export default {
  notes_edit,
  star_icon,
  star_gray,
  CrossButton,
  up_icon,
  down_icon,
  up_icon,
  right_arrow,
  down_white_icon,
  review_place,
  review,
  uncompleted_icon,
  completed_icon,
  Session_icon,
  noteIcon,
  protien_icon,
  left_arrow,
  carb_icon,
  fat_icon,
  fiber_icon,
  white_star,
  edit_white,
  plus_icon,
  file,
  shoulder,
  leg,
  tab_workout,
  chat_icon,
  tab_member,
  trainer_comment,
  member_comment,
  write_icon,
  req_write,
  req_confirm,
};
